{
  "name": "@agent-ops/runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/bin.ts",
    "workflow": "bun run src/workflow-cli.ts",
    "build": "bun build src/bin.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "diff": "^8.0.3",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/diff": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
